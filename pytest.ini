[pytest]
# Only tests/ holds real tests. Without this, pytest would also collect any
# script in the root whose name happens to start with test_.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
